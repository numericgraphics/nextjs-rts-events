import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles((theme) => ({
    container: {
        width: '100%',
        marginBottom: '1rem'

    },
    linearProgress: {
        height: '1.8rem',
        borderRadius: '1rem'
    }

}))
